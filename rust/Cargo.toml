[package]
name = "sdg_tools"
version = "0.1.0"
edition = "2021"
description = "Parse Scopus SDG query files and check papers against them — Rust, SIMD (AVX2), zero dependencies"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
