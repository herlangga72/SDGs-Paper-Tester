# The sqlite DB is generated by sdg2sqlite.py at image build time —
# never ship it in the build context (it's also gitignored).
engine/data/sdg_queries.sqlite3

.git
.github
__pycache__/
*.pyc
# Only exclude the Rust build output; rust/src and Cargo files are needed
# to build the web server inside the image.
rust/target/
target/
.DS_Store
Thumbs.db
